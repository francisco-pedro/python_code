x_company = 'FREECompany'
lista = []
#Interação para pegar cada letra da variavel name

for l in x_company: 
# Add cada letra da variavel name a variavel lista por meio do loop for com a variavel l
    lista.append(l)

# Mostrando o resultado
print(lista)